# user's real config (may contain domains / chat ids); ship only the example
skills/cyrano/config.json

# runtime dedup ledger
skills/cyrano/.state/
**/.state/

# python
__pycache__/
*.pyc

# test scratch
skills/cyrano/tests/sample_brief.txt

# agent handoff notes (may contain private working context — never publish)
HANDOFF.md
**/HANDOFF.md

# os
.DS_Store
Thumbs.db
